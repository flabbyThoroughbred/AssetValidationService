def load_assets(dataFile: str) -> dict:
    """
    Take an input file <json> and load as 
    dictionary.
    :param dataFile: <str> must be a .json file

    :returns: <dict> dictionary object loaded from json
    """
    pass

"""
 validate assets - am i validating the assets that are
 being loaded in?
 Does the loader also add to the db after validation? 
 store errored values and list why their error specifics.


 The general gist is to be able to ingest, validate and store
 assets and asset versions.
"""
